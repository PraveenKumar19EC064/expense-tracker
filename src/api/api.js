// const { GoogleSpreadsheet } = require("google-spreadsheet");
// import keys from "keys.json";

const auth = async () => {
  console.log("1000");
  //   const doc = new GoogleSpreadsheet("");
  //   await doc.useServiceAccountAuth(keys);
  //   await doc.loadInfo()
};

// loadInfo(async () => await doc.loadInfo());

export { auth };
